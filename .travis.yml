language: python
python:
  - '3.6'
services:
  - postgresql
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
install:
  - pip install -f requirements.txt
  - pip install coverage coveralls
script:
  - coverage run --source=ObsDataRest -m pytest .
after_success:
  - coveralls
